---
# Base configuration playbook - Only common packages
# Use this for minimal VM setup without Docker

- name: Base system configuration
  hosts: deployment_vm
  become: yes
  
  roles:
    - role: common
      tags:
        - common
